# ubuntu-drivers install nvidia:570-open
kubuntu-desktop
systemd-boot
arch-install-scripts
tree
cryptsetup cryptsetup-initramfs
ifconfig
system-analyze
net-tools
rsync
git
code
wget
gpg
curl
tee
apt-transport-https
code
zsh
python3
python3-pip
fzf
ripgrep
autojump
steam-installer
xclip
qimgv
dotnet-sdk-8.0
# nvidia-driver-libs:i386
borgbackup
shutter
btop
cifs-utils
# For resolving DNS from command line using the `dig` command
dnsutils
# Requires docker signing key to be installed and apt source to be setup
docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-pluginpython3-full
rsync
btrfs-compsize # compare real vs compressed size of files
gh # github cli
smartmontools # ssd and harddrive health `smartctl -a /dev/sdd`, `smartctl -x /dev/sdd`
# dkms
# firefox-devedition